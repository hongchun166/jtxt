package com.linkb.jstx.dialog;

import android.app.Dialog;
import android.content.Context;
import android.support.annotation.NonNull;

import com.linkb.R;
import com.linkb.jstx.dialog.base.BaseDialog;

public class ChatReadDeleteDig extends BaseDialog {

    public ChatReadDeleteDig(Context context) {
        super(context);
    }

    public ChatReadDeleteDig(Context context, int theme) {
        super(context, theme);
    }
}
